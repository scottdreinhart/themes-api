/**
 * Infrastructure layer barrel export.
 * Re-exports adapters, configuration, and external service clients.
 *
 * Usage: import { config, logger } from '@/infra'
 */

export { config } from './config.js'
export { logger } from './logger.js'
