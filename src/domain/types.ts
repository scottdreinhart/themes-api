/**
 * Central type definitions — pure domain types, no framework dependencies.
 * theme packs, catalog items, asset manifests, download tokens, purchase records, and install states
 */

// Add domain-specific types here
export {}
