/**
 * Domain constants — magic numbers & config extracted to a single source of truth.
 */

export const DEFAULT_PAGE_SIZE = 25
export const MAX_PAGE_SIZE = 100
export const DEFAULT_TIMEOUT_MS = 5000
